# API base URL of the FastAPI backend.
# Leave unset in development: the Vite dev server proxies /api/v1 to http://127.0.0.1:8000.
# Set it for production builds, e.g. VITE_API_URL=https://your-api.example.com/api/v1
VITE_API_URL=
