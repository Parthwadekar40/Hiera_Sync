"""Domain engines: risk prediction, approval workflow and analytics.

These are pure-ish modules (no FastAPI imports) so they can be unit-tested, benchmarked
and reused by the scheduler, the API and offline scripts alike.
"""
